{
  "description": "Configuration for Kafka Sales Data Producer",
  "environments": {
    "local": {
      "bootstrap_servers": "localhost:9092",
      "topic": "sales_events",
      "interval": 2.0,
      "burst_mode": false
    },
    "msk": {
      "bootstrap_servers": "REPLACE_WITH_MSK_BOOTSTRAP_SERVERS",
      "topic": "sales_events",
      "interval": 1.0,
      "burst_mode": false,
      "security_protocol": "SSL"
    }
  },
  "default": {
    "bootstrap_servers": "localhost:9092",
    "topic": "sales_events",
    "count": null,
    "interval": 1.0,
    "burst_mode": false
  },
  "sample_usage": {
    "commands": [
      "python kafka_producer.py --count 100",
      "python kafka_producer.py --interval 0.5 --verbose",
      "python kafka_producer.py --burst --count 1000",
      "python kafka_producer.py --config producer_config.json"
    ]
  }
}
