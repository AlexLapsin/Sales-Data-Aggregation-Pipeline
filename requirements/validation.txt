# Configuration Validator Dependencies
# Install with: pip install -r requirements-config-validator.txt

# Core dependencies (always required)
PyYAML>=6.0
ipaddress>=1.0.23

# Cloud service dependencies (optional - for connectivity testing)
# Install only if you need to test connectivity to these services

# AWS connectivity testing
boto3>=1.26.0

# Database connectivity testing
psycopg2-binary>=2.9.0

# Snowflake connectivity testing
snowflake-connector-python>=3.0.0

# Kafka connectivity testing
kafka-python>=2.0.0

# Databricks connectivity testing (via REST API)
requests>=2.28.0

# Testing dependencies (for running test suite)
pytest>=7.0.0
pytest-cov>=4.0.0

# Development dependencies
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0
